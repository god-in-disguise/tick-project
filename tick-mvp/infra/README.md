# Infra

Deployment target:

- DigitalOcean for backend processes.
- Vercel for frontend PWA.
- Docker Compose first.
- Terraform can be added here once the service shape is stable.

Keep secrets out of git. Use `.env.example` as the contract.

